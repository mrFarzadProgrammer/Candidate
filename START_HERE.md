# 📝 دستورالعمل نهایی برای آپلود و دیپلوی

<div dir="rtl">

## 🎯 خلاصه وضعیت

✅ **پروژه کامل شد و تست شد**  
✅ **Git مقداردهی شد و commit های لازم انجام شد**  
✅ **مستندات کامل نوشته شد**  
✅ **آماده برای آپلود به GitHub**  
✅ **آماده برای دیپلوی در سرور رایگان**

---

## 📤 مرحله 1: آپلود به GitHub

### الف) ایجاد Repository جدید

1. به [github.com/new](https://github.com/new) برید
2. اطلاعات را وارد کنید:
   - **Repository name:** `election-bot-system` (یا هر نامی که می‌خواهید)
   - **Description:** `🗳️ سیستم جامع مدیریت بات‌های انتخاباتی تلگرام`
   - **Visibility:** Public (برای استفاده رایگان از Render)
   - ⚠️ **Don't initialize** with README (ما قبلاً داریم)
3. Create repository

### ب) پوش کردن کد

بعد از ساخت repository، این دستورات رو در PowerShell اجرا کنید:

```powershell
cd C:\Workspace\candidate

# حذف remote قبلی (اگر وجود داره)
git remote remove origin

# اضافه کردن remote جدید
# جایگزین کنید با آدرس repository خودتون:
git remote add origin https://github.com/YOUR_USERNAME/election-bot-system.git

# پوش کردن
git push -u origin main

# پوش کردن tag
git push origin v1.0.0
```

**نکته:** اگر Git از شما username/password خواست:
- Username: نام کاربری GitHub
- Password: Personal Access Token (نه رمز عبور اصلی!)

**ساخت Token:**
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Select scopes: `repo`
4. Generate token و کپی کنید

---

## 🚀 مرحله 2: دیپلوی در Render.com

### الف) ساخت حساب Render

1. به [render.com](https://render.com) برید
2. Sign Up → **Continue with GitHub**
3. به Render اجازه دسترسی بدهید

### ب) ایجاد PostgreSQL Database

1. Dashboard → **New** → **PostgreSQL**
2. تنظیمات:
   ```
   Name: election-bot-db
   Database: election_bot
   User: election_user
   Region: Frankfurt (نزدیک‌ترین به ایران)
   Plan: Free
   ```
3. **Create Database**
4. بعد از ساخت، **Internal Database URL** را کپی کنید و ذخیره کنید
   - مثلاً: `postgresql://election_user:xxx@xxx.frankfurt-postgres.render.com/election_bot`

### ج) تولید Secret Keys

در ترمینال اجرا کنید:
```powershell
cd C:\Workspace\candidate
.venv\Scripts\python.exe generate_secrets.py
```

دو کلید تصادفی برایتان generate می‌شود. آنها را کپی کنید.

### د) ایجاد Web Service

1. Dashboard → **New** → **Web Service**
2. **Connect repository** → مخزن GitHub خود را انتخاب کنید
3. تنظیمات:
   ```
   Name: election-bot-system
   Region: Frankfurt (همان region دیتابیس)
   Branch: main
   Root Directory: (خالی بگذارید)
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
   Plan: Free
   ```

4. **Environment Variables** را اضافه کنید:
   
   کلیک روی **Add Environment Variable**:
   
   ```
   Key: DATABASE_URI
   Value: <internal-database-url-که-کپی-کردید>
   
   Key: ADMIN_SECRET_KEY
   Value: <کلید-اول-از-generate_secrets>
   
   Key: CANDIDATE_SECRET_KEY
   Value: <کلید-دوم-از-generate_secrets>
   ```

5. **Create Web Service**

### ه) منتظر بمانید...

Build حدود 3-5 دقیقه طول می‌کشد. در بخش **Logs** می‌توانید پیشرفت را ببینید.

وقتی پیام `==> Your service is live 🎉` را دیدید، مرحله بعد!

### و) مقداردهی دیتابیس

1. در صفحه Web Service → بالای صفحه → **Shell** (کنار Logs)
2. دستور زیر را وارد کنید:
   ```bash
   python init_db.py
   ```
3. باید پیام "✅ مقداردهی اولیه با موفقیت انجام شد!" را ببینید

---

## 🎉 مرحله 3: تست سیستم

### دسترسی به پنل‌ها

لینک اپلیکیشن شما مثل این است:
```
https://election-bot-system.onrender.com
```

**صفحات:**
- صفحه اصلی: `https://YOUR-APP.onrender.com/`
- پنل ادمین: `https://YOUR-APP.onrender.com/admin/`
- پنل نماینده: `https://YOUR-APP.onrender.com/candidate/`

### ورود به پنل ادمین

```
نام کاربری: admin
رمز عبور: admin123
```

⚠️ **مهم:** بعد از ورود حتماً رمز عبور را تغییر دهید!

### ایجاد اولین نماینده

1. در پنل ادمین → **نمایندگان** → **ایجاد نماینده جدید**
2. فرم را پر کنید:
   ```
   نام کامل: علی احمدی
   نام کاربری: ali_ahmadi
   رمز عبور: 123456
   تلفن: 09121234567
   ```
3. ذخیره

### ساخت بات در تلگرام

1. تلگرام را باز کنید
2. به `@BotFather` پیام دهید
3. دستور `/newbot` را بزنید
4. نام بات: `علی احمدی`
5. Username بات: `ali_ahmadi_election_bot`
6. **توکن** را کپی کنید (مثل: `1234567890:ABCdef...`)

### راه‌اندازی بات

1. پنل ادمین → **نمایندگان** → کنار نام علی احمدی → **راه‌اندازی بات**
2. فرم را پر کنید:
   ```
   توکن بات: <توکنی-که-از-BotFather-گرفتید>
   نام کاربری بات: ali_ahmadi_election_bot
   ```
3. ذخیره

### تست بات

1. در تلگرام به `@ali_ahmadi_election_bot` برید
2. دکمه **Start** را بزنید
3. منوی inline ظاهر می‌شود! 🎉

---

## ✅ چک‌لیست نهایی

- [ ] Repository در GitHub ساخته شد
- [ ] کد به GitHub push شد
- [ ] Tag v1.0.0 push شد
- [ ] حساب Render ساخته شد
- [ ] PostgreSQL Database ایجاد شد
- [ ] Web Service ایجاد شد
- [ ] Environment Variables تنظیم شد
- [ ] Build موفق بود
- [ ] دیتابیس init شد
- [ ] به پنل ادمین وارد شدید
- [ ] رمز عبور admin تغییر یافت
- [ ] نماینده ایجاد شد
- [ ] بات در BotFather ساخته شد
- [ ] بات راه‌اندازی شد
- [ ] بات در تلگرام تست شد

---

## 🆘 رفع مشکلات رایج

### خطا: Build failed
- Logs را بررسی کنید
- مطمئن شوید `requirements.txt` درست است
- Python version را چک کنید (`runtime.txt`)

### خطا: Database connection failed
- DATABASE_URI را چک کنید
- مطمئن شوید PostgreSQL فعال است
- Internal Database URL را (نه External) استفاده کردید

### بات کار نمی‌کند
- توکن بات را چک کنید
- مطمئن شوید در Shell دستور `python init_db.py` را زده‌اید
- Logs را بررسی کنید

### سرور خاموش می‌شود
- پلن Free بعد از 15 دقیقه خاموش می‌شود
- برای جلوگیری از UptimeRobot استفاده کنید

---

## 📞 نیاز به کمک؟

اگر در هر مرحله مشکلی داشتید:

1. **Logs را چک کنید** (در Render)
2. **مستندات را بخوانید:**
   - `GITHUB_DEPLOY_GUIDE.md` - راهنمای کامل
   - `DEPLOYMENT.md` - جزئیات بیشتر
   - `README.md` - اطلاعات پروژه
3. **GitHub Issues** - سوال بپرسید

---

## 🎯 بعد از دیپلوی موفق

حالا که سیستم آماده است، می‌توانید:

1. **نمایندگان بیشتری اضافه کنید**
2. **پلن‌ها را مدیریت کنید**
3. **امکانات جدید اضافه کنید**
4. **نسخه‌های بعدی را توسعه دهید**

برای شروع توسعه نسخه‌های بعدی:
- `CONTRIBUTING.md` را بخوانید
- از `push_to_github.bat` برای push استفاده کنید
- Render به صورت خودکار auto-deploy می‌کند

---

**موفق باشید! 🚀🎉**

</div>
