# سند کامل: سیستم Trial و کنترل کامل ادمین

## ✅ تغییرات Database

### 1. جدول `plan_purchases` - فیلدهای جدید:
```python
is_trial = db.Column(db.Boolean, default=False)
  # آیا این خرید یک دوره تست (Trial) است؟
  
trial_used = db.Column(db.Boolean, default=False)
  # آیا در این خرید، سهمیه trial استفاده شده؟
  
custom_duration_days = db.Column(db.Integer)
  # مدت دلخواه ادمین (اولویت با duration_days پلن)
  # مثال: ادمین می‌تونه بگه "7 روز رایگان"
  
admin_granted = db.Column(db.Boolean, default=False)
  # آیا این پلن توسط ادمین به صورت رایگان اعطا شده؟
  
admin_note = db.Column(db.Text)
  # یادداشت ادمین (مثلاً: "هدیه برای کمپین موفق", "تبلیغات")
```

### 2. جدول `candidates` - فیلد جدید:
```python
has_used_trial = db.Column(db.Boolean, default=False)
  # آیا این کاندیدا قبلاً از Trial استفاده کرده؟
  # برای جلوگیری از سوء استفاده (هر کاندیدا فقط 1 بار)
```

---

## 🎯 قابلیت‌های جدید برای ادمین

### 1. **اعطای Trial 3 روزه**
```
ادمین → کاندیداها → [کاندیدای مورد نظر] → "فعال‌سازی Trial"

تنظیمات:
- مدت: 3 روز (پیش‌فرض)
- پلن: PROFESSIONAL (با همه امکانات)
- قیمت: 0 تومان
- is_trial: True
- trial_used: True
- has_used_trial (کاندیدا): True

محدودیت:
- هر کاندیدا فقط یکبار
- چک شدن has_used_trial قبل از اعطا
```

### 2. **اعطای پلن رایگان (هر مدت دلخواه)**
```
ادمین → کاندیداها → [کاندیدای مورد نظر] → "اعطای رایگان"

فرم:
┌─────────────────────────────────────┐
│ انتخاب پلن: [BASIC ▼]             │
│ مدت (روز): [30]                    │
│ قیمت: 0 تومان (رایگان)             │
│ یادداشت ادمین:                      │
│ [________________________]          │
│                                     │
│ دلیل اعطا:                          │
│ ☐ تبلیغات                          │
│ ☐ جایزه                            │
│ ☐ کمپین موفق                       │
│ ☐ تست                              │
│ ☐ سایر                             │
│                                     │
│ [اعطای رایگان] [لغو]               │
└─────────────────────────────────────┘

تنظیمات ثبت شده:
- custom_duration_days: مقدار وارد شده
- payment_amount: 0
- payment_status: 'completed'
- payment_method: 'free'
- admin_granted: True
- admin_note: متن یادداشت
```

### 3. **تمدید یا ارتقا با هر مدت دلخواه**
```
ادمین → اشتراک‌ها → [خرید مورد نظر] → "ویرایش"

می‌تواند تغییر دهد:
- تاریخ شروع
- تاریخ پایان
- مدت اشتراک
- پلن (ارتقا/کاهش)
- قیمت (حتی 0 تومان)
- وضعیت پرداخت
```

### 4. **نمایش تاریخچه کامل**
```
ادمین → گزارشات → "تاریخچه Trial"

نمایش:
┌────────┬─────────────┬────────┬──────────┬──────────┐
│ کاندیدا │ پلن         │ مدت    │ تاریخ    │ وضعیت    │
├────────┼─────────────┼────────┼──────────┼──────────┤
│ احمدی  │ PROFESSIONAL│ 3 روز  │ 1404/08/15│ Trial ✅ │
│ محمدی  │ GOLD        │ 7 روز  │ 1404/08/10│ رایگان 🎁│
│ رضایی   │ BASIC       │ 60 روز │ 1404/08/05│ ویژه 🏆 │
└────────┴─────────────┴────────┴──────────┴──────────┘
```

---

## 🚀 مراحل پیاده‌سازی

### گام 1: ✅ Database Migration (انجام شد)
- فیلدها به جدول اضافه شدند
- اسکریپت `add_trial_fields.py` اجرا شد

### گام 2: Admin Panel - صفحه فعال‌سازی Trial
```python
@app.route('/candidate/<int:id>/activate-trial', methods=['POST'])
@admin_required
def activate_trial(id):
    """فعال‌سازی Trial 3 روزه"""
    candidate = Candidate.query.get_or_404(id)
    
    # چک کردن استفاده قبلی
    if candidate.has_used_trial:
        flash('این کاندیدا قبلاً از Trial استفاده کرده است', 'danger')
        return redirect(url_for('candidates'))
    
    # پلن PROFESSIONAL
    professional_plan = Plan.query.filter_by(code='PROFESSIONAL').first()
    
    # ایجاد Trial
    trial_purchase = PlanPurchase(
        candidate_id=candidate.id,
        plan_id=professional_plan.id,
        purchase_date=datetime.utcnow(),
        start_date=datetime.utcnow(),
        end_date=datetime.utcnow() + timedelta(days=3),
        payment_amount=0,
        payment_status='completed',
        payment_method='free',
        is_trial=True,
        trial_used=True,
        is_active=True,
        notes='Trial 3 روزه رایگان'
    )
    
    candidate.has_used_trial = True
    
    db.session.add(trial_purchase)
    db.session.commit()
    
    flash('Trial 3 روزه با موفقیت فعال شد!', 'success')
    return redirect(url_for('candidates'))
```

### گام 3: Admin Panel - صفحه اعطای رایگان
```python
@app.route('/candidate/<int:id>/grant-free', methods=['GET', 'POST'])
@admin_required
def grant_free_plan(id):
    """اعطای پلن رایگان با هر مدت دلخواه"""
    candidate = Candidate.query.get_or_404(id)
    
    if request.method == 'POST':
        plan_id = request.form.get('plan_id')
        duration_days = int(request.form.get('duration_days'))
        admin_note = request.form.get('admin_note')
        
        plan = Plan.query.get(plan_id)
        
        free_purchase = PlanPurchase(
            candidate_id=candidate.id,
            plan_id=plan.id,
            purchase_date=datetime.utcnow(),
            start_date=datetime.utcnow(),
            end_date=datetime.utcnow() + timedelta(days=duration_days),
            payment_amount=0,
            payment_status='completed',
            payment_method='free',
            custom_duration_days=duration_days,
            admin_granted=True,
            admin_note=admin_note,
            is_active=True
        )
        
        db.session.add(free_purchase)
        db.session.commit()
        
        flash(f'پلن {plan.name} برای {duration_days} روز رایگان اعطا شد!', 'success')
        return redirect(url_for('candidates'))
    
    plans = Plan.query.all()
    return render_template('admin/grant_free.html', candidate=candidate, plans=plans)
```

### گام 4: Candidate Panel - دکمه Trial
```python
@app.route('/activate-trial', methods=['POST'])
@login_required
def activate_trial():
    """فعال‌سازی خودکار Trial توسط کاندیدا"""
    candidate = Candidate.query.get(session['candidate_id'])
    
    if candidate.has_used_trial:
        flash('شما قبلاً از دوره تست رایگان استفاده کرده‌اید', 'warning')
        return redirect(url_for('view_plans'))
    
    # ... کد فعال‌سازی Trial (مشابه ادمین)
    
    flash('🎉 دوره تست 3 روزه رایگان شما فعال شد!', 'success')
    return redirect(url_for('dashboard'))
```

---

## 📊 UI/UX پیشنهادی

### در صفحه پلن‌ها (Candidate):
```html
{% if not candidate.has_used_trial %}
<div class="trial-banner">
    <h2>🎁 3 روز تست رایگان!</h2>
    <p>همه امکانات پلن حرفه‌ای را بدون پرداخت امتحان کنید</p>
    <ul>
        <li>✅ بات تلگرام اختصاصی</li>
        <li>✅ پیام نامحدود</li>
        <li>✅ مدیریت 3 کانال</li>
        <li>✅ پست زمان‌بندی شده</li>
        <li>✅ آمار و گزارش</li>
    </ul>
    <form method="POST" action="{{ url_for('activate_trial') }}">
        <button type="submit" class="btn btn-success btn-lg">
            🚀 شروع رایگان 3 روزه
        </button>
    </form>
    <p style="font-size: 12px; color: #666;">
        بدون نیاز به کارت اعتباری • لغو آسان
    </p>
</div>
{% else %}
<div class="info-box">
    <i class="fas fa-info-circle"></i>
    شما قبلاً از دوره تست استفاده کرده‌اید
</div>
{% endif %}
```

### در داشبورد ادمین:
```html
<table>
    <tr>
        <td>احمد محمدی</td>
        <td>09121234567</td>
        <td>
            {% if not candidate.has_used_trial %}
            <button onclick="activateTrial({{candidate.id}})" 
                    class="btn btn-sm btn-info">
                🎁 فعال‌سازی Trial
            </button>
            {% endif %}
            
            <button onclick="grantFree({{candidate.id}})" 
                    class="btn btn-sm btn-success">
                💰 اعطای رایگان
            </button>
        </td>
    </tr>
</table>
```

---

## 📈 گزارشات پیشنهادی برای ادمین

### 1. آمار Trial:
```
┌─────────────────────────────────┐
│ آمار دوره تست (Trial)           │
├─────────────────────────────────┤
│ کل استفاده‌ها: 127             │
│ فعال: 23                        │
│ منقضی شده: 104                  │
│ نرخ تبدیل: 38% → خرید          │
│ میانگین زمان تبدیل: 1.8 روز     │
└─────────────────────────────────┘
```

### 2. آمار اعطای رایگان:
```
┌─────────────────────────────────┐
│ اعطاهای رایگان ادمین            │
├─────────────────────────────────┤
│ تعداد: 34                       │
│ ارزش کل: 68,000,000 تومان      │
│ دلایل:                          │
│   • تبلیغات: 15                │
│   • جایزه: 8                   │
│   • تست: 11                    │
└─────────────────────────────────┘
```

---

## ✅ Checklist پیاده‌سازی

- [x] Database Migration
- [ ] Admin Panel - فعال‌سازی Trial
- [ ] Admin Panel - اعطای رایگان
- [ ] Admin Panel - گزارش‌ها
- [ ] Candidate Panel - دکمه Trial
- [ ] Candidate Panel - بنر Trial
- [ ] Email notification (اختیاری)
- [ ] تست کامل

---

آماده‌ایم برای ادامه؟ 🚀
