# گزارش پیاده‌سازی سیستم پلن‌های اشتراک و محدودیت‌ها

## تاریخ: ۲۲ آبان ۱۴۰۴

---

## ✅ کارهای انجام شده

### 1. سیستم محدودیت‌ها (Restriction Enforcement)

#### 1.1 محدودیت برنامه‌ها (`/programs`)
**فایل:** `candidate_panel/app.py` - خط 206-226

**عملکرد:**
- قبل از افزودن برنامه جدید، `candidate.can_add_program()` چک می‌شود
- اگر به حد مجاز رسیده باشد:
  - پیام flash نمایش داده می‌شود
  - کاربر به صفحه `/plans` هدایت می‌شود
  
**پیام‌ها:**
- با پلن: "شما به حداکثر تعداد برنامه‌ها (X) رسیده‌اید. برای افزودن برنامه بیشتر، پلن خود را ارتقا دهید."
- بدون پلن: "شما پلن فعالی ندارید. برای افزودن برنامه، لطفاً یک پلن خریداری کنید."

#### 1.2 محدودیت دفاتر (`/headquarters`)
**فایل:** `candidate_panel/app.py` - خط 242-270

**عملکرد:**
- قبل از افزودن دفتر جدید، `candidate.can_add_headquarters()` چک می‌شود
- همان رفتار محدودیت برنامه‌ها

**پیام‌ها:**
- با پلن: "شما به حداکثر تعداد دفاتر (X) رسیده‌اید. برای افزودن دفتر بیشتر، پلن خود را ارتقا دهید."
- بدون پلن: "شما پلن فعالی ندارید. برای افزودن دفتر، لطفاً یک پلن خریداری کنید."

#### 1.3 محدودیت پیام‌ها (Telegram Bot)
**فایل:** `bot_engine/telegram_bot.py` - خط 225-241

**عملکرد:**
- قبل از ذخیره پیام کاربر، `candidate.can_add_message()` چک می‌شود
- اگر سهمیه تمام شده باشد:
  - پیام در دیتابیس ذخیره نمی‌شود
  - پیام زیر به کاربر تلگرام نمایش داده می‌شود
  
**پیام:**
```
⚠️ متأسفانه ظرفیت دریافت پیام تکمیل شده است.
لطفاً بعداً دوباره تلاش کنید.
```

---

### 2. داشبورد کاندیدا - بنرها و ویجت‌ها

#### 2.1 Route داشبورد (`/dashboard`)
**فایل:** `candidate_panel/app.py` - خط 106-166

**متغیرهای جدید ارسال شده به template:**
- `active_plan`: پلن فعلی کاندیدا (از `get_active_plan()`)
- `latest_purchase`: آخرین خرید فعال
- `plan_usage`: دیکشنری حاوی اطلاعات استفاده:
  ```python
  {
      'messages': {'used': 45, 'limit': 100, 'percentage': 45.0},
      'programs': {'used': 3, 'limit': 5, 'percentage': 60.0},
      'headquarters': {'used': 2, 'limit': 3, 'percentage': 66.67}
  }
  ```

**محاسبات:**
- پیام‌های ماه جاری: از اول ماه تا امروز
- تعداد برنامه‌ها: تمام برنامه‌های کاندیدا
- تعداد دفاتر: تمام دفاتر کاندیدا

#### 2.2 بنرهای هشدار
**فایل:** `templates/candidate/dashboard.html` - خط 104-188

**حالت 1: پلن منقضی شده** (days_left <= 0)
- پس‌زمینه قرمز روشن
- آیکون: exclamation-triangle
- دکمه: "تمدید یا ارتقای پلن"

**حالت 2: کمتر از 7 روز مانده** (days_left < 7)
- پس‌زمینه زرد/نارنجی
- آیکون: clock
- نمایش تعداد روزهای باقیمانده
- دکمه: "تمدید پلن"

**حالت 3: بدون پلن فعال**
- پس‌زمینه قرمز
- آیکون: shopping-cart
- دکمه: "مشاهده پلن‌ها و خرید"

**هشدارهای استفاده (Usage Warnings):**
- وقتی 90% یا بیشتر از هر محدودیت استفاده شده باشد
- بنر زرد کوچک برای هر مورد
- لینک مستقیم به صفحه پلن‌ها

#### 2.3 ویجت وضعیت پلن
**فایل:** `templates/candidate/dashboard.html` - خط 195-213

**محتوا:**
- نام پلن فعلی
- تعداد روزهای باقیمانده (با رنگ‌بندی)
  - سبز: بیش از 7 روز
  - زرد: 1-7 روز
  - قرمز: منقضی شده
- پس‌زمینه آبی gradient

---

### 3. منوی Navigation

**فایل:** تمام صفحات `templates/candidate/*.html`

**لینک جدید اضافه شده:**
```html
<a href="{{ url_for('view_plans') }}" class="menu-item">
    <i class="fas fa-tags"></i>
    <span>پلن‌های اشتراک</span>
</a>
```

**صفحات به‌روزرسانی شده:**
- ✅ dashboard.html
- ✅ bot.html
- ✅ profile.html
- ✅ programs.html
- ✅ headquarters.html
- ✅ messages.html
- ✅ resume.html
- ✅ plans.html (جدید)

---

## 📊 جریان کاربری (User Flow)

### سناریو 1: کاندیدا بدون پلن

1. ورود به داشبورد
2. **بنر قرمز نمایش داده می‌شود:** "شما پلن فعالی ندارید"
3. کلیک روی "مشاهده پلن‌ها و خرید" → `/plans`
4. مقایسه پلن‌ها
5. کلیک "درخواست مشاوره"
6. پر کردن فرم (شماره تماس، زمان مناسب، توضیحات)
7. ثبت درخواست در دیتابیس
8. ادمین در `/subscriptions` درخواست را می‌بیند
9. ادمین با کاندیدا تماس می‌گیرد
10. پس از پرداخت، ادمین پلن را فعال می‌کند
11. کاندیدا به تمام امکانات دسترسی پیدا می‌کند

### سناریو 2: رسیدن به حد مجاز برنامه‌ها

1. کاندیدا پلن BASIC دارد (5 برنامه)
2. قبلاً 5 برنامه ثبت کرده
3. تلاش برای افزودن برنامه ششم
4. سیستم `can_add_program()` را چک می‌کند → False
5. **پیام flash نمایش داده می‌شود:** "شما به حداکثر تعداد برنامه‌ها (5) رسیده‌اید..."
6. Redirect به `/plans`
7. مشاهده پلن PROFESSIONAL با برنامه نامحدود
8. درخواست ارتقا

### سناریو 3: نزدیک شدن به انقضا

1. 5 روز تا انقضای پلن مانده
2. ورود به داشبورد
3. **بنر زرد نمایش داده می‌شود:** "تنها 5 روز تا انقضای پلن شما باقی مانده"
4. **ویجت پلن:** تعداد روز با رنگ زرد نمایش داده می‌شود
5. کلیک "تمدید پلن"
6. انتخاب همان پلن یا ارتقا

### سناریو 4: استفاده 90% از سهمیه پیام

1. کاندیدا پلن BASIC دارد (100 پیام/ماه)
2. 92 پیام دریافت کرده
3. ورود به داشبورد
4. **بنر کوچک زرد:** "شما 92% از سهمیه پیام خود را مصرف کرده‌اید"
5. **ویجت پیام:** نوار پیشرفت قرمز
6. کلیک "ارتقای پلن"

---

## 🎨 طراحی UI

### رنگ‌بندی وضعیت‌ها

**سبز (Green):**
- Hex: #10b981, #059669
- معنی: وضعیت خوب، بیش از 30% باقیمانده

**زرد (Yellow/Orange):**
- Hex: #f59e0b, #d97706, #fef3c7
- معنی: هشدار، 7-30% باقیمانده

**قرمز (Red):**
- Hex: #ef4444, #dc2626, #fee2e2
- معنی: خطر، کمتر از 7% یا صفر

### Gradient Backgrounds

```css
/* پلن منقضی */
background: linear-gradient(135deg, #fee2e2, #fecaca);
border: 3px solid #ef4444;

/* هشدار انقضا */
background: linear-gradient(135deg, #fef3c7, #fde68a);
border: 3px solid #f59e0b;

/* ویجت پلن */
background: linear-gradient(135deg, #dbeafe, #bfdbfe);
```

---

## 📝 کدهای کلیدی

### چک محدودیت در Backend

```python
# برنامه‌ها
if not candidate.can_add_program():
    active_plan = candidate.get_active_plan()
    if active_plan:
        flash(f'محدودیت {active_plan.max_programs}', 'warning')
    else:
        flash('پلن فعال ندارید', 'danger')
    return redirect(url_for('view_plans'))
```

### محاسبه استفاده

```python
from datetime import datetime

# پیام‌های ماه جاری
month_start = datetime.utcnow().replace(day=1, hour=0, minute=0, second=0, microsecond=0)
messages_this_month = Message.query.filter_by(
    candidate_id=candidate.id
).filter(Message.created_at >= month_start).count()

# درصد استفاده
percentage = (messages_this_month / active_plan.max_messages * 100) if active_plan.max_messages > 0 else 0
```

### نمایش بنر در Template

```html
{% if active_plan and latest_purchase %}
    {% set days_left = latest_purchase.days_remaining() %}
    
    {% if days_left <= 0 %}
        <!-- بنر قرمز -->
    {% elif days_left < 7 %}
        <!-- بنر زرد -->
    {% endif %}
{% else %}
    <!-- بنر بدون پلن -->
{% endif %}
```

---

## 🧪 تست‌های پیشنهادی

### 1. تست محدودیت برنامه
```
1. ایجاد candidate test
2. فعال‌سازی پلن BASIC (5 برنامه)
3. افزودن 5 برنامه → موفق
4. افزودن برنامه ششم → باید مسدود شود
5. چک پیام flash
6. چک redirect به /plans
```

### 2. تست محدودیت پیام
```
1. فعال‌سازی پلن BASIC (100 پیام/ماه)
2. ارسال 100 پیام از بات تلگرام
3. ارسال پیام 101 → باید مسدود شود
4. چک پیام در تلگرام
5. ماه بعد → سهمیه reset می‌شود
```

### 3. تست هشدار انقضا
```
1. ایجاد purchase با end_date = امروز + 5 روز
2. ورود به dashboard
3. چک نمایش بنر زرد
4. چک ویجت "5 روز"
5. تغییر end_date به گذشته
6. چک بنر قرمز
```

### 4. تست هشدار 90%
```
1. پلن با 10 برنامه
2. افزودن 9 برنامه (90%)
3. ورود به dashboard
4. چک نمایش بنر هشدار
5. افزودن برنامه دهم
6. چک بنر همچنان نمایش داده می‌شود (100%)
```

---

## 🚀 استقرار (Deployment)

### تغییرات دیتابیس
هیچ migration جدید لازم نیست. تمام جداول از قبل ایجاد شده‌اند.

### فایل‌های تغییر یافته
```
candidate_panel/app.py           (route dashboard + محدودیت‌ها)
bot_engine/telegram_bot.py       (محدودیت پیام)
templates/candidate/dashboard.html (بنرها + ویجت‌ها)
templates/candidate/bot.html      (منو)
templates/candidate/profile.html  (منو)
templates/candidate/programs.html (منو)
templates/candidate/headquarters.html (منو)
templates/candidate/messages.html (منو)
templates/candidate/resume.html   (منو)
templates/candidate/plans.html    (جدید - جلسه قبل)
```

### Restart Services
```bash
# Admin Panel
python admin_panel/app.py

# Candidate Panel  
python candidate_panel/app.py

# Bot (اگر در حال اجراست)
# بسته شدن و راه‌اندازی مجدد
```

---

## 📈 آمار پیاده‌سازی

- **خطوط کد اضافه شده:** ~300
- **فایل‌های تغییر یافته:** 10
- **Route های به‌روزرسانی شده:** 3
- **Template های به‌روزرسانی شده:** 8
- **زمان پیاده‌سازی:** ~2 ساعت

---

## ✨ ویژگی‌های کلیدی

1. ✅ **محدودیت واقعی:** سیستم جلوی عملیات غیرمجاز را می‌گیرد
2. ✅ **Feedback واضح:** پیام‌های مفید به کاربر
3. ✅ **Redirect هوشمند:** هدایت به صفحه خرید
4. ✅ **نمایش بصری:** بنرها و ویجت‌های جذاب
5. ✅ **Real-time:** محاسبات لحظه‌ای
6. ✅ **Responsive:** قابل استفاده در موبایل
7. ✅ **رنگ‌بندی:** سبز/زرد/قرمز برای وضعیت‌های مختلف
8. ✅ **UX خوب:** جریان خرید آسان

---

## 🎯 مراحل بعدی (اختیاری)

1. **نوتیفیکیشن انقضا:**
   - ایمیل/SMS در 7، 3، 1 روز قبل از انقضا
   - استفاده از Celery برای task scheduling

2. **تجدید خودکار:**
   - اضافه کردن درگاه پرداخت
   - فیلد `auto_renew` در PlanPurchase

3. **Discount Codes:**
   - جدول `DiscountCode`
   - اعمال تخفیف در خرید

4. **Usage Analytics:**
   - نمودار استفاده روزانه
   - پیش‌بینی زمان اتمام سهمیه

5. **Trial Period:**
   - 7 روز رایگان برای کاربران جدید
   - محدودیت‌های کاهش یافته

6. **Referral System:**
   - کد معرف
   - تخفیف برای معرف و معرفی شده

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. چک کنید پلن فعالی وجود دارد
2. تاریخ انقضا را بررسی کنید
3. محدودیت‌ها در `database/models.py` → `can_add_*()` تعریف شده‌اند
4. لاگ‌های Flask را بررسی کنید

---

**تاریخ تکمیل:** ۲۲ آبان ۱۴۰۴  
**نسخه:** 1.0  
**وضعیت:** ✅ آماده برای استفاده در production
