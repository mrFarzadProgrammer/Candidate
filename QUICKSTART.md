# 🚀 راهنمای شروع سریع

<div dir="rtl">

## 📥 نصب سریع (5 دقیقه)

### گام 1: دانلود پروژه
```bash
git clone https://github.com/Farzad93/Cafe_Bots_Project.git
cd candidate
```

### گام 2: نصب Python و وابستگی‌ها

**Windows:**
```powershell
# ایجاد محیط مجازی
python -m venv venv

# فعال‌سازی
.\venv\Scripts\Activate.ps1

# نصب کتابخانه‌ها
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### گام 3: راه‌اندازی
```bash
# مقداردهی دیتابیس
python init_db.py

# اجرای سیستم
python main.py
```

---

## 🔑 دسترسی اولیه

### پنل ادمین
- **آدرس**: http://localhost:5000
- **نام کاربری**: `admin`
- **رمز عبور**: `admin123`

### پنل نماینده
- **آدرس**: http://localhost:5001
- (نام کاربری و رمز توسط ادمین ایجاد می‌شود)

---

## 📝 سناریوی استفاده

### برای مدیر سیستم:

1. **ورود به پنل ادمین** (localhost:5000)
2. **ایجاد نماینده جدید**:
   - از منو: نماینده‌ها > ایجاد نماینده
   - وارد کردن اطلاعات
3. **دریافت توکن بات از BotFather**:
   - در تلگرام به [@BotFather](https://t.me/BotFather) مراجعه کنید
   - دستور `/newbot` را ارسال کنید
   - نام و username بات را وارد کنید
   - توکن دریافتی را کپی کنید
4. **راه‌اندازی بات**:
   - در لیست نماینده‌ها، روی "راه‌اندازی بات" کلیک کنید
   - توکن و username را وارد کنید
   - بات به‌صورت خودکار راه‌اندازی می‌شود
5. **فعال‌سازی پلن** (اختیاری):
   - انتخاب نماینده
   - فعال کردن پلن‌های مورد نیاز

### برای نماینده:

1. **ورود با نام کاربری و رمز** (دریافتی از ادمین)
2. **تکمیل پروفایل**:
   - نام، شهر، حوزه انتخابیه
   - بیوگرافی
   - آپلود عکس
3. **افزودن رزومه**:
   - عنوان، توضیحات، سال
4. **ثبت برنامه‌های انتخاباتی**:
   - موضوع، دسته‌بندی، توضیحات
5. **ثبت آدرس ستادها**:
   - نام ستاد، آدرس، تلفن
   - موقعیت جغرافیایی (اختیاری)
6. **مشاهده و پاسخ به پیام‌ها**
   - بستگی به پلن فعال دارد

---

## 🤖 استفاده از بات (توسط مردم)

1. **پیدا کردن بات نماینده**:
   - جستجوی username بات در تلگرام
   - یا اسکن QR Code

2. **شروع مکالمه**:
   - دکمه START یا دستور `/start`

3. **مشاهده اطلاعات**:
   - رزومه نماینده
   - برنامه‌های انتخاباتی
   - آدرس ستادها
   - اطلاعات تماس

4. **ارسال پیام** (اگر فعال باشد):
   - انتخاب "ارسال پیام"
   - نوشتن پیام
   - ارسال

---

## ⚙️ تنظیمات پیشرفته

### تغییر رمز ادمین:
```python
# در کنسول Python
python
>>> from admin_panel.app import app, db, Admin
>>> from werkzeug.security import generate_password_hash
>>> with app.app_context():
...     admin = Admin.query.filter_by(username='admin').first()
...     admin.password = generate_password_hash('رمز-جدید')
...     db.session.commit()
```

### استفاده از PostgreSQL:
```bash
# نصب
pip install psycopg2-binary

# تنظیم .env
DATABASE_URI=postgresql://user:pass@localhost/election_bot
```

### اضافه کردن پلن جدید:
در `config/settings.py` به لیست `DEFAULT_PLANS` اضافه کنید.

---

## 🐛 حل مشکلات رایج

### خطا: Module not found
```bash
pip install -r requirements.txt --upgrade
```

### خطا: Port already in use
```bash
# تغییر پورت در app.py
app.run(port=5002)
```

### بات راه‌اندازی نمی‌شود
- توکن را بررسی کنید
- از فعال بودن بات در BotFather اطمینان حاصل کنید
- لاگ‌های کنسول را بررسی کنید

### دیتابیس خطا می‌دهد
```bash
# حذف و ایجاد مجدد
rm election_bot.db
python init_db.py
```

---

## 📞 پشتیبانی

- **ایمیل**: support@example.com
- **گیت‌هاب**: https://github.com/Farzad93/Cafe_Bots_Project
- **Issues**: https://github.com/Farzad93/Cafe_Bots_Project/issues

---

## 🎓 مفاهیم کلیدی

### ادمین
مدیر سیستم که نماینده‌ها را ایجاد و بات‌ها را راه‌اندازی می‌کند.

### نماینده
کاندیدای انتخاباتی که بات اختصاصی دارد و محتوا را مدیریت می‌کند.

### بات
ربات تلگرامی که با مردم در تعامل است و اطلاعات نماینده را نمایش می‌دهد.

### پلن
بسته امکانات قابل فروش به نماینده‌ها.

### دیتابیس مرکزی
همه بات‌ها به یک دیتابیس متصل هستند.

---

## ✅ چک‌لیست راه‌اندازی

- [ ] Python نصب شده
- [ ] محیط مجازی ایجاد شده
- [ ] وابستگی‌ها نصب شدند
- [ ] دیتابیس مقداردهی شد
- [ ] پنل ادمین در حال اجراست
- [ ] پنل نماینده در حال اجراست
- [ ] نماینده ایجاد شد
- [ ] توکن بات از BotFather دریافت شد
- [ ] بات راه‌اندازی شد
- [ ] محتوا توسط نماینده اضافه شد
- [ ] بات در تلگرام تست شد

---

<p align="center">
  <strong>✨ آماده استفاده است!</strong>
</p>

</div>
